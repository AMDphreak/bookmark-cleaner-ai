I noticed my bookmarks are organized differently and not synchronized between my different browsers. Make an ai-powered app to intelligently assist me with synchronizing my bookmarks between Firefox, Google Chrome, and Microsoft Edge. Support multi-profile setup so user1 in firefox, chrome, and edge can sync account, and user2 can sync their accounts, and user3, etc, with an interface to map the accounts onto each other. Support the option of connecting directly to the accounts via API as well. Inspect the bookmarks either locally within the browser files on disk or after downloading to a local cache via API (whichever was configured), provide a paneled interface to browse and compare the bookmarks, and a separate window with a blank bookmark view panel to the left and an AI chat to the right to discuss the bookmarks and plan the integration. Allow the user to select a model under the chat input box from openrouter to perform the analysis. Include a button above the bookmark view to toggle sources on and off and initiate the analysis of the bookmarks. The AI analysis and plan generator should generate a detailed analysis in the chat response and a plan to integrate the bookmarks from all selected providers into a single unified folder structure. Prior to AI analysis, the app should check the validity of the bookmarks and provide a view in a separate window that shows all sources' links' colorized statuses in a bookmark tree (expressed via text color), including which links are dead (red), which links have descriptions that no longer match the live web page (yellow), which links have an invalid path but exist on a valid domain/subdomain (orange), and which items are duplicates within the profile's set (and show the duplicate's path in a column to the right).

Make this app in Tauri and solidjs and suid.dev. let me know if we need to pick a different component library. solidui seems good too.

MIT license

application should request API key from user upon attempting to submit the first request. AI window should have a settings button that opens settings to configure the api key as well. api key should be stored in settings file, not sourced from an env file.

Add support for openai api, claude api, grok api, gemini api, and groq api, since it has really fast inference. i think it may also be cheaper. Encourage user to use groq gateway with gpt-oss-120b if they can afford a small cost or Gemini 2.5 Pro if they need a free AI.